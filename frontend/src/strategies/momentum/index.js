import { momentumContent } from "./content";
import { momentumVisuals } from "./images";
import { momentumConfig } from "./config";

export const momentumStrategy = {
  content: momentumContent,
  visuals: momentumVisuals,
  config: momentumConfig,
};
