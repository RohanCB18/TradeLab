import { rsiContent } from "./content";
import { rsiVisuals } from "./images";
import { rsiConfig } from "./config";

export const rsiStrategy = {
  content: rsiContent,
  visuals: rsiVisuals,
  config: rsiConfig,
};
