import { macdContent } from "./content";
import { macdVisuals } from "./images";
import { macdConfig } from "./config";

export const macdStrategy = {
  content: macdContent,
  visuals: macdVisuals,
  config: macdConfig,
};
