import { scalpingContent } from "./content";
import { scalpingVisuals } from "./images";
import { scalpingConfig } from "./config";

export const scalpingStrategy = {
  content: scalpingContent,
  visuals: scalpingVisuals,
  config: scalpingConfig,
};
