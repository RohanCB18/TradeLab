import { meanReversionContent } from "./content";
import { meanReversionVisuals } from "./images";
import { meanReversionConfig } from "./config";

export const meanReversionStrategy = {
  content: meanReversionContent,
  visuals: meanReversionVisuals,
  config: meanReversionConfig,
};
